# Optional: production API origin (no trailing slash). Leave unset for local dev — Vite proxies /api to localhost:3000.
# VITE_API_BASE_URL=https://your-api.railway.app

# Set to true to enforce the 3-brief trial cap while running `npm run dev` (mimics production).
# VITE_FORCE_TRIAL_LIMIT=true
